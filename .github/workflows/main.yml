name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      run: |
        ./generate.sh
        chmod 777 start.sh
        ./start.sh
        echo Add other actions to build,
        echo test, and deploy your project.
